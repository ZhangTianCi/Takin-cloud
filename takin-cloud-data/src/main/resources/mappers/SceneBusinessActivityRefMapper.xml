<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.shulie.takin.cloud.data.mapper.mysql.SceneBusinessActivityRefMapper">
    <insert id="batchInsert" parameterType="java.util.List">
        insert into t_scene_business_activity_ref
        (scene_id, business_activity_id, business_activity_name, application_ids, bind_ref, goal_value)
        values
        <foreach collection="items" item="item" separator=",">
            (
            #{item.sceneId},
            #{item.businessActivityId},
            #{item.businessActivityName},
            #{item.applicationIds},
            #{item.bindRef},
            #{item.goalValue}
            )
        </foreach>
    </insert>
</mapper>